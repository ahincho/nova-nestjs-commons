/**
 * Constantes del módulo de enmascaramiento.
 *
 * Se definen en un archivo separado para evitar dependencias circulares
 * entre el módulo, el servicio y el interceptor.
 */

/** Token de inyección para las opciones del módulo de enmascaramiento. */
export const MASK_MODULE_OPTIONS = 'MASK_MODULE_OPTIONS';
